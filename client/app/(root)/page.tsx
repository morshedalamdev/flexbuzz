import Image from "next/image";

export default function HomePage() {
  return (
      <main className="flex w-full min-h-[calc(100dvh-48px)] max-w-3xl mx-auto flex-col items-center justify-between bg-white dark:bg-black">
        
      </main>
  );
}
